version: 2.1

orbs:
  node: circleci/node@6.1.0

commands:
  install-and-cache:
    parameters:
      app-dir:
        type: string
    steps:
      - node/install-packages:
          pkg-manager: npm
          app-dir: << parameters.app-dir >>
          cache-path: << parameters.app-dir >>/node_modules
          override-ci-command: npm ci

jobs:
  test-backend:
    docker:
      - image: cimg/node:20.17.0 # Updated to match server version
      - image: cimg/postgres:14.1
        environment:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U test_user -d test_db"]
          interval: 10s
          timeout: 5s
          retries: 5
    environment:
      TEST_DB_HOST: localhost
      TEST_DB_NAME: test_db
      TEST_DB_USER: test_user
      TEST_DB_PASSWORD: test_password
      TEST_DB_PORT: 5432
      NODE_OPTIONS: "--max-old-space-size=4096" # Increased heap size for Node 20
    steps:
      - checkout
      - install-and-cache:
          app-dir: server
      - run:
          name: Wait for PostgreSQL
          command: |
            for i in `seq 1 10`;
            do
              nc -z localhost 5432 && echo Success && exit 0
              echo -n .
              sleep 1
            done
            echo Failed waiting for PostgreSQL && exit 1
      - run:
          name: Run Backend Tests
          command: cd server && npm run test
          no_output_timeout: 5m

  test-frontend:
    docker:
      - image: cimg/node:20.17.0
    environment:
      NODE_OPTIONS: "--max-old-space-size=4096"
    steps:
      - checkout
      - install-and-cache:
          app-dir: client
      - run:
          name: Run Frontend Tests
          command: cd client && npm run test
          no_output_timeout: 5m

  build:
    docker:
      - image: cimg/node:20.17.0
    environment:
      NODE_OPTIONS: "--max-old-space-size=4096"
    steps:
      - checkout
      - install-and-cache:
          app-dir: "."
      - run:
          name: Build Client
          command: |
            cd client
            npm run build
            tar -czf build.tar.gz build/
      - run:
          name: Build Server
          command: |
            cd server
            npm run build
            npx tsc migrations/*.ts --outDir dist/migrations
            tar -czf dist.tar.gz dist/ package*.json
      - persist_to_workspace:
          root: .
          paths:
            - client/build.tar.gz
            - server/dist.tar.gz
            - server/package*.json

  deploy:
    machine: 
      image: ubuntu-2204:current
    steps:
      - attach_workspace:
          at: /home/circleci/project
      - add_ssh_keys:
          fingerprints:
            - "SHA256:6+qHFQDbqCYFzFzHfY7fpBaly2rEB83DUxswOGj6dkI"
      - run:
          name: Notify Deploy Start
          command: |
            echo "Starting deployment to $SERVER_IP"
            date > deploy-start.txt
      - run:
          name: Check Node Version on Server
          command: |
            NODE_VERSION=$(ssh $SERVER_USER@$SERVER_IP "node --version")
            if [[ "$NODE_VERSION" != "v20.17.0" ]]; then
              echo "Warning: Server Node.js version ($NODE_VERSION) differs from CI version (v20.17.0)"
              exit 1
            fi
      - run:
          name: Deploy to Server
          command: |
            # Decompress client build
            cd /home/circleci/project/client
            tar -xzf build.tar.gz
            
            # Decompress server files
            cd /home/circleci/project/server
            tar -xzf dist.tar.gz
            
            # Create backup of current deployment
            ssh $SERVER_USER@$SERVER_IP "cd $SERVER_DIR && \
              tar -czf backup-\$(date +%Y%m%d-%H%M%S).tar.gz client server"
            
            # Deploy new version
            scp -r /home/circleci/project/client/build/* $SERVER_USER@$SERVER_IP:$SERVER_DIR/client
            scp -r /home/circleci/project/server/dist/* /home/circleci/project/server/package*.json $SERVER_USER@$SERVER_IP:$SERVER_DIR/server
            
            # Install dependencies and restart services
            ssh $SERVER_USER@$SERVER_IP "cd $SERVER_DIR/server && \
              export NODE_OPTIONS='--max-old-space-size=4096' && \
              npm ci --production && \
              NODE_ENV=production npm run migrate && \
              pm2 restart $SERVER_DIR/server/ecosystem.config.js && \
              sudo systemctl restart nginx"
      - run:
          name: Verify Deployment
          command: |
            for i in {1..12}; do
              if curl -s https://taskmaster.mrspinn.ca/api/health | grep -q "ok"; then
                echo "Deployment verified successfully"
                exit 0
              fi
              echo "Waiting for service to be ready..."
              sleep 10
            done
            echo "Deployment verification failed"
            exit 1

workflows:
  test-build-and-deploy:
    jobs:
      - test-backend:
          filters:
            branches:
              only: master
      - test-frontend:
          filters:
            branches:
              only: master
      - build:
          requires:
            - test-backend
            - test-frontend
          filters:
            branches:
              only: master
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master